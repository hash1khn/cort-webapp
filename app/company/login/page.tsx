"use client";

import AuthLoginForm from "../../components/AuthLoginForm";

export default function CompanyLoginPage() {
  return (
    <AuthLoginForm
      title="Company Portal"
      subtitle="Sign in to your corporate dashboard"
      submitButtonText="Sign In to Portal"
    />
  );
}
